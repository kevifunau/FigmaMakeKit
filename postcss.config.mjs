// Standard Tailwind CSS v4 PostCSS config.
// Note: with the @tailwindcss/vite plugin this file is optional, but it is
// shipped so the same stylesheets work in non-Vite tooling too.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
